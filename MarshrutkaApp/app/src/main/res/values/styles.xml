<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/theme_primary</item>
        <item name="colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="colorAccent">@color/theme_accent_2</item>
        <item name="android:textColorLink">@color/theme_accent_2</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    <style name="AppTheme" parent="AppTheme.Base">

    </style>
    <style name="TabIndicator" parent="ActionBarThemeOverlay"/>
    <style name="ActionBarThemeOverlay" parent="">
        <item name="android:textColorPrimary">#fff</item>
        <item name="colorControlNormal">@color/actionBarIconColor</item>
        <item name="colorControlHighlight">#3fff</item>
    </style>
    <style name="ActionBarPopupThemeOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="ActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#fff</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="DestionationPointText">
        <item name="android:textSize">16sp</item>
        <item name="android:padding">3dp</item>
        <item name="android:textColor">@color/list_item_text_color</item>
    </style>

    <style name="QueryByDestinationLabelText">
        <item name="android:textSize">18sp</item>
        <item name="android:padding">3dp</item>
    </style>

    <style name="ListFastScrollStyle">
    </style>

</resources>
